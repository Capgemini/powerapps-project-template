{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "command": "if (Test-Path ./debug) { Remove-Item ./debug -Confirm:$false }; New-Item ./debug -ItemType \"directory\";",
      "label": "clean-debug",
      "problemMatcher": []
    }
  ]
}
